# Server environment variables example
# Copy this file to .env and fill values before starting the server

# Gemini / Google Generative AI key
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-lite

# Server
PORT=4000

# SMTP (for sending escalation emails)
# Example for an SMTP provider (e.g. SendGrid, Mailgun SMTP, Gmail SMTP with app password)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# From address used in outgoing emails
EMAIL_FROM=no-reply@example.com

# Frontend API URL (used by client-side API calls)
VITE_API_URL=http://localhost:4000
# Google Gemini API Configuration
# Get your free API key: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your_api_key_here

# Server Configuration
PORT=4000
NODE_ENV=development
